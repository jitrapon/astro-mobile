# Specification: M-1 part 2 — shared data layer (Ktor, serialization, contract models)

> Per-branch working file owned by the `spec-development` skill. Each branch
> overwrites the section bodies; this file in `main` is a skeleton that
> documents the canonical structure so every branch follows the same shape.

## 1. Overview

M-1 ("Set up astro-mobile project structure") landed only its first half: the `shared` / `androidApp` / `iosApp` restructure, followed by CI and toolchain work. Its remaining scope — a real networking and serialization stack in the shared module, plus data models shaped to the BFF's calendar screen contract — was never built, so the shared module still holds nothing but the original POC's login/greeting sample code. This branch closes that gap so it lands before M-2 rather than inside it, and additionally settles how dependencies are wired together in the shared module.

M-1 part 2 is deliberately separated from M-2 by risk class as well as size: this is KMP toolchain work (multiplatform HTTP engines, the serialization compiler plugin, iOS target linking), while M-2 is UI architecture (navigation, app shell, SDUI registry, view models). An iOS linking failure should not share a PR with the SDUI registry design.

## 2. Objective

The shared module builds for both the Android and iOS targets with a working HTTP client, JSON serialization, a dependency-injection graph, an API client for the BFF's range-based calendar screen endpoint, and hand-written data models matching that contract — all exercised by a contract-parity test rather than left as unused code, so M-2's view model and M-5's switch to generated models are a swap rather than a rewrite.

## 3. Requirements & Context

**Contract version.** Write the models against `schemaVersion` **0.2.0**, not the earlier "interim" shape the milestone plan's M-1 row describes. In 0.2.0 the response envelope carries a required `theme` reference and an optional theme document, and every screen operation accepts a shared `knownTheme` parameter; `resolvedPreferences.theme` is no longer a `light | dark | system` string enum. Models written to the 0.1.0-era shape would turn M-5's move to generated models into a rewrite.

**Model coverage.** The canonical event and calendar core, plus the month and agenda view-models. The API client targets the BFF's range-based calendar screen endpoint, parameterized by view, start, end, timezone, and locale.

**Contract vendoring and parity gating.** Mirror the pattern `astro-web` already established: vendor the BFF OpenAPI contract as a plain checked-in file (not a submodule — the BFF repo does not exist yet), add the upstream planning/docs repo as a submodule to provide the upstream mirror, and pair them with a shared-module test that holds all four artifacts to the same contract version: the vendored spec, the upstream mirror, the fixture, and the hand-written models. The parity test must decode the canonical month-screen example fixture through the real models and fail on drift between any of the four. This is what makes the branch self-verifying instead of shipping dead code with no caller until M-2.

**Dependency injection — user-directed scope addition beyond the issue.** Adopt **Koin** as the DI framework for the shared module on this branch, and wire the HTTP client, API client, and repository graph through it. Rationale: it is KMP-first, needs no KSP or compiler-plugin in the build, and keeps the iOS-facing surface small. The accepted trade-off is runtime resolution with no compile-time graph validation. Requirements:

- The DI graph is declared in shared common code; each platform contributes only its platform-specific pieces (notably the HTTP engine) and starts the graph at app launch.
- The iOS side must be able to initialize and resolve from the graph through the shared framework's public surface — no Swift-side reimplementation of the graph.
- Adopting Koin here means DI is settled before M-2 builds view models on top of it.
- Record the choice as an architecture decision in the planning repo, since that is where this project's architectural decisions live.

**Out of scope.** The BFF contract submodule and generated-client codegen (deferred to M-5, since the BFF is not yet scaffolded). Any UI, navigation, SDUI registry, or calendar view model (M-2). Google login and the auth flow (M-5).

**POC cleanup.** The leftover greeting / login sample types in the shared module must be removed, or explicitly retained with a stated reason.

**Downstream trigger.** This branch introduces the project's first real product dependencies, which is the trigger condition recorded on the deferred issue for a PR-blocking Gradle SCA (SBOM-based) gate. That issue's trigger was written against M-2 and should be re-pointed at this work.

**Blocking relationship.** This branch blocks M-2, whose calendar view model consumes these models and this DI graph.

**Local verification constraint.** The shared module's iOS unit tests must be verified with the simulator ARM64 target on Apple Silicon; the x64 iOS test task fails locally with a CPU-type error and is not a valid signal.

## 4. Implementation Plan and Progress Tracking (for agent)

Standing constraints that apply to every item below:

- **No hardcoded dispatchers or blocking in `commonMain`.** `DispatchersIOInCommonMain`, `RunBlockingInCommonMain`, and `MainScopeWithoutCancel` are blocking Detekt rules here, and `.semgrep/coroutines.yml`'s `kotlin-hardcoded-dispatcher` catches the top-level / builder-argument cases Detekt misses. The API client is `suspend`-only; Ktor is already main-safe, so no `withContext(Dispatchers.IO)` wrapper is needed or permitted.
- **No cleartext HTTP literal.** `.semgrep/astro-mobile.yml`'s `kotlin-cleartext-http-url` fires on an `http://` string literal in Kotlin. The base URL is injected, never a literal default.
- **No Detekt baseline and no `@Suppress`.** Findings are refactored away (`checkNoDetektBaseline` + `ForbiddenSuppress` enforce this).
- **All new dependency versions go in `gradle/libs.versions.toml`,** not inline — this branch is where the catalog stops being lint-only.

Ordered items:

- [x] 1. Add the product-dependency versions and aliases to `gradle/libs.versions.toml` (kotlinx-serialization runtime + its Kotlin-pinned Gradle plugin, kotlinx-coroutines, Ktor client with the OkHttp and Darwin engines, Koin) and add the `kotlin-serialization` Gradle plugin artifact to the root `buildscript` classpath alongside the existing Kotlin/AGP entries. Rewrite the catalog's header comment, which currently declares the catalog deliberately lint-only.
- [x] 2. Apply the serialization plugin in `shared/build.gradle.kts` and wire the source-set dependencies: `commonMain` gets ktor-client-core + content-negotiation + the JSON serialization converter + kotlinx-serialization-json + koin-core; `androidMain` gets the OkHttp engine; `iosMain` gets the Darwin engine; `commonTest` gets ktor-client-mock, kotlinx-coroutines-test, and koin-test. Do not add `export(...)` to the framework config — Koin and Ktor types must stay off the iOS framework's public surface (item 17 gives Swift a Kotlin facade instead).
- [x] 3. Add `android.permission.INTERNET` to `androidApp/src/main/AndroidManifest.xml`. Without it every Ktor request on Android fails at runtime with a permission error no unit test would surface.
- [x] 4. Add `docs/astro-docs` as a git submodule (`git@github.com:jitrapon/astro-docs.git`), matching astro-web's placement, and vendor the two contract artifacts from it: `contracts/astro-bff/openapi.yaml` (byte-identical copy) and the month-screen example fixture into `shared/src/commonTest/resources/contract/`. Document the `git submodule update --init` bootstrap step wherever the repo's setup instructions live.
- [x] 5. Check the submodule out on the `verify-android-common` job in `.github/workflows/ci.yml` — only that job, since the mirror comparison runs in the host-portable half. **Not** via checkout's `submodules: true`: `astro-docs` is private and checkout's default `GITHUB_TOKEN` is scoped to this repo, so resolving the submodule there fails the clone outright. Mirror astro-web's working shape instead — keep the checkout at `submodules: false` and fetch the submodule in two following steps: `webfactory/ssh-agent` (SHA-pinned, per the `actions-pin` gate) loading a read-only deploy key from an `ASTRO_DOCS_DEPLOY_KEY` repo secret, then an explicit `git submodule update --init --recursive`. The key goes into an agent rather than checkout's `ssh-key:` input because that input also governs the main repo's clone and would demand one credential valid for both. Give `verify-ios` an explicit `submodules: false` too — nothing in the `verifyIos` aggregate reads the mirror. Leave every `.github/workflows/security.yml` checkout at `submodules: false`; those jobs never read the mirror and the explicit `false` is a deliberate posture there.
- [x] 6. Register a `verifyVendoredContractParity` task in the root `build.gradle.kts` following the repo's existing drift-guard pattern (`checkNoDetektBaseline`, `verifyKtfmtAlignment`): fail if the vendored `openapi.yaml` is not byte-identical to the submodule mirror, or if the vendored fixture differs from the mirror fixture after dropping the fixture-local `_comment` annotation key. Fail with an explicit "run `git submodule update --init`" message when the mirror is absent, rather than silently passing. Keep it configuration-cache-safe (capture `File`s / `String`s; do the comparison in `doLast`).
- [x] 7. Wire `verifyVendoredContractParity` into every subproject's `check` and add `":" to "verifyVendoredContractParity"` to the `androidCommonVerification` list in the root build's partition data. The `verifyCheckPartition` guard fails until both halves are done, so this item is not complete until the guard reports OK.
- [x] 8. Add the test-resource generator: a `:shared` build-time task that emits a Kotlin source file into a generated `commonTest` source directory, registered on the `commonTest` source set with the compile task depending on it. This item lands the mechanism and the two simplest constants — the vendored fixture JSON and the contract's declared `info.version`. Emit long text as a concatenated list of chunks, not one literal: the JVM constant pool caps a single string constant at 65535 bytes and the contract is already within a few KB of that. The generator exists because `kotlin.test` has no multiplatform resource-loading API and the iOS simulator test binary's working directory is not a reliable base for file reads; the same loader is what the reserved `calendar-layout-golden` corpus will need on M-2.
- [x] 9. Extend the generator to extract and embed the **contract facts** the conformance test (item 21) asserts against, so that test is contract-derived rather than a restatement of the client. Two groups: (a) *request* facts for the calendar screen operation — its path, required query-parameter names, optional query-parameter names, the `view` enum, and each parameter's declared **type and format** (`start` / `end` are `string` / `format: date`, `dayCount` is an integer bounded 1–7), since the wire format is where a client silently diverges from the contract; (b) *response* facts — the `required:` field list and any enum values for each response schema the models cover, month and agenda alike. Two extraction traps to handle explicitly: the operation's parameter list reaches `knownTheme` through a `$ref` to a shared component, so a naive slice of the inline `parameters:` block silently omits it; and the body schemas hang off a discriminator mapping (`calendar.month.v1` / `calendar.agenda.v1`) rather than sitting inline. Slice the YAML textually as astro-web does — the contract is inert YAML no build step reads, and adding a YAML parser to serve one test is not worth the dependency.
- [x] 10. Add the response envelope and theme models to `commonMain` at `schemaVersion` 0.2.0: the envelope's `schemaVersion` / `serverTime` / `locale` / `timeZone` / required `theme` / `screen` plus the **optional** `themeDocument`, the `ThemeRef`, and the theme document with its colour, shadow, and font token maps. `themeDocument` is nullable by contract — it is absent precisely on the `knownTheme` cache-hit path, which is the common case in production, so it must never become a required field or an unconditional dereference. Declare a single `SUPPORTED_SCHEMA_VERSION` constant holding `"0.2.0"` that the client, the version guard, and the parity test all read.
- [x] 11. Add the canonical calendar core and the month view-model to `commonMain`: `Calendar` with its six-slot colour block, the presented-event wrapper (`itemType` + `presentation` + `props`), the timed / all-day event props discriminated on `kind`, the month body's range / anchor / header / calendars / events, and the month presentation components. Model the discriminated unions as sealed hierarchies rather than stringly-typed fields.
- [x] 12. Add the agenda view-model plus the screen chrome shared by both views: screen id/title, navigation destinations, the view switcher and its view-selection union, the action union, and `ResolvedPreferences` — which at 0.2.0 carries `weekStart` / `chipStyle` / `chipDensity` and deliberately **no** `theme` field. Add a comment naming why the absence is load-bearing (a screen with no preferences block could not discover the theme when the reference lived there), so a future edit does not helpfully re-add it. Note that no agenda fixture exists upstream, so the agenda models' only mechanical gate is item 21's contract-derived requiredness check — write them against the contract's `AgendaBody` schema, not by analogy with the month body.
- [x] 13. Add the shared `Json` configuration and the `CalendarScreenApi` client in `commonMain`: one `suspend` function hitting the calendar screen operation against an injected base URL and returning the existing sealed `Result<T>`. Cover the full request surface the contract declares — the five required query parameters (view, start, end, tz, locale), the optional `dayCount` (time-grid column count, ignored by other views), and the shared `knownTheme` parameter — with `start` / `end` as date-only values, not instants. Note the deliberate casing split: the request's `view` enum is lower-case (`timegrid`) while the response's view-selection discriminator is camel-case (`timeGrid`); they are different vocabularies and must not be unified. Production decoding is lenient (unknown keys ignored) so a BFF field addition does not break a shipped client; the parity test uses its own strict `Json` (item 19). Map transport and decode failures onto `Result.Error` rather than letting exceptions escape — but **`CancellationException` is not a failure and must be rethrown**, never converted into a `Result`. A blanket `catch (e: Exception)` around a suspending Ktor call swallows cancellation and breaks structured concurrency: when M-2 cancels a superseded range or view request, the coroutine would deliver an error to a caller that has already moved on instead of terminating. Rethrow cancellation before the mapping catch. This cannot be left to the lint gate — `CancellationExceptionSwallowed` sits in the `structured-coroutines` WARN tier at `weight: 0` in `config/detekt/detekt.yml`, so it reports without failing the build.
- [x] 14. Add a schema-version guard at the decode boundary: after decoding, `CalendarScreenApi` returns `Result.Error` — never `Result.Success` — when the envelope's `schemaVersion` is not exactly `SUPPORTED_SCHEMA_VERSION`, with an error carrying both the received and the supported version. This is the counterweight to item 13's lenient decoding: with unknown keys ignored, a future incompatible response would otherwise deserialize into these hand-written models with defaults and be returned as a success. astro-web already does this at its own fetch boundary (`assertSupportedSchemaVersion`, exact equality); mobile mirrors the semantics and differs only in surfacing it through `Result<T>` instead of throwing.
- [x] 15. Add `CalendarScreenRepository` in `commonMain`, wrapping `CalendarScreenApi` — the repository half of the graph §3 requires and the repository-wraps-a-data-source pattern the project already documents. It is a **stateless forwarding boundary** on this branch: it holds no cached screen, no in-flight generation counter, and no refresh policy. That is a deliberate choice, not an omission. A cache here would have to key on the full request identity (view, start, end, timezone, locale, `dayCount`, `knownTheme`) and define what happens when a slower earlier response lands after a newer one; get either wrong and it hands the UI a screen belonging to a different range or view. Neither can be settled without M-2's view model, and a cache with no consumer is the unexercised dead code this branch exists to avoid. Its value today is the boundary: M-2 consumes a repository rather than reaching past it into the API client, and adds cache identity and ordering when it has a consumer to define them for. Say so in the class's KDoc so the absent cache reads as a decision to the next author.
- [x] 16. Declare the Koin graph: a `commonMain` module providing the `HttpClient`, the `Json`, the API client, and the repository, plus an `expect`/`actual` platform module supplying the `HttpClientEngine` (OkHttp on Android, Darwin on iOS). Expose a single `initKoin(...)` entry point taking the base URL, so neither platform reimplements the graph.
- [x] 17. Start the graph on both platforms. Android: add an `Application` subclass calling `initKoin`, registered via `android:name` in the manifest. iOS: add a Kotlin facade on the shared framework's public surface that Swift calls to initialize and to resolve the repository, so no Koin or Ktor type crosses the framework boundary; call it from the SwiftUI app entry point. The graph itself is already clean — `initKoin` takes a `String` and returns nothing, and the modules are `internal` — but Ktor is **not** off the boundary yet: `CalendarScreenApi`'s public constructor takes an `HttpClient`, which puts `SharedKtor_client_coreHttpClient` and everything it reaches into the generated header. Closing that means narrowing `CalendarScreenApi` and the repository's constructor to `internal`, since the graph is the only thing that constructs either; verify by grepping the linked framework's `Headers/shared.h` for `Ktor_`, not by inspection. Add an `iosTest` that drives that same facade — init, resolve, tear down — so the iOS half of the graph is exercised by a test that already runs in CI, not only by whatever a developer happens to launch.
- [x] 18. Remove the POC leftovers (lands **after** item 19 — deleting the greeting tests empties
  `commonTest`, and Gradle fails a test task that discovers nothing): `Greeting`, `LoginDataSource`, `LoginDataValidator`, `LoginRepository`, `LoggedInUser`, their three greeting tests, and the now-unused JUnit dependency on the `androidHostTest` source set. Rewrite `iosApp`'s `iOSApp.swift` / `ContentView.swift`, which construct the login types today and will not compile otherwise. Reduce the view to the minimum that proves the facade resolved — it must render some state derived from the resolved repository (not a static string), so that the mandatory simulator launch in §5 is evidence of resolution rather than merely of not crashing. This is a compile-and-verification necessity, not new product UI. **Retain `Result<T>`** and `Platform` / `Utils`: `Result<T>` is a documented project pattern and is the API client's return type, and the `expect`/`actual` pair is the documented platform-abstraction example. State the retention reason in `Result<T>`'s KDoc.
- [x] 19. Write the contract-parity test in `commonTest`: decode the embedded fixture through the real models with a **strict** `Json` (unknown keys rejected) after stripping the fixture-local `_`-prefixed annotation keys, assert the decoded `schemaVersion` equals `SUPPORTED_SCHEMA_VERSION` equals `"0.2.0"`, assert the contract's embedded declared version agrees, and assert the delivered `themeDocument`'s `id@version` matches the envelope's `theme` reference. The strict decode is what turns an unmodelled contract field into a failure instead of a silently dropped field.
- [x] 20. Add mutation cases to the parity test proving it fails on the drift it claims to catch — at minimum: the envelope loses `theme`, the theme document's version diverges from the reference, an event's `kind` disagrees with its temporal fields, the body props gain an unmodelled field, and a preference leaves its enumerated set. Add the inverse case as well: the same fixture with `themeDocument` removed entirely must still decode successfully with its `theme` reference intact, since that is what the BFF returns on a `knownTheme` cache hit. Without the mutations the suite passes on an untouched fixture and proves nothing.
- [x] 21. Add the contract-conformance test, asserting the models and client against item 9's embedded contract facts rather than against a hand-copied list. Request side, over a captured `MockEngine` request rather than by inspection: the path the client builds matches the contract's operation path, the parameters it always sends equal the contract's required set, every conditional parameter is in the optional set, and the `view` values it can emit are a subset of the contract's enum. Assert the **values** too, not only the names — a wrong encoding passes every name-level check and then 400s against the real BFF: `start` and `end` are serialized date-only (no time component, no offset, no `Z`) per the contract's `format: date`, `dayCount` is sent only for the time-grid view and only within the contract's 1–7 bounds, and an absent optional parameter is *omitted from the query string* rather than sent as an empty or `null` value. Response side: for each modelled response schema — **including agenda, which has no fixture** — the model's field set matches the contract's `required:` list, and every modelled enum matches the contract's. This is the only mechanical gate the agenda models get, and it is the only check that catches a response-schema change the single month fixture does not happen to exercise. Full response-schema validation across every declared screen branch is deliberately **not** attempted: that is what `./gradlew openApiGenerate` gives for free at M-5, and astro-web reached the same conclusion for the same reason (a branch nothing exercises is an untested claim dressed as coverage).
- [x] 22. Add behaviour tests in `commonTest` driving Ktor's `MockEngine`, asserting on both halves of each exchange — the request the client actually emitted and the `Result` it returned. Request half: a month-view call carries date-only `start` / `end` and no `dayCount`; a time-grid call carries `dayCount`; a call made with no known theme omits `knownTheme` from the query string entirely rather than sending it empty. Response half: a 200 decodes to `Result.Success`; a 200 whose envelope omits `themeDocument` still decodes to `Result.Success` with the theme reference retained; a non-2xx or malformed body yields `Result.Error` rather than throwing; and an otherwise-valid envelope carrying a `schemaVersion` above and below the supported one yields `Result.Error` naming both versions (item 14). Cover the repository over the same engine: it forwards the call and returns the API client's `Result` unchanged on both the success and the failure path, and two successive calls with different request parameters each return their own response — the assertion that pins item 15's statelessness, so a cache added later without a key fails here instead of silently serving another request's screen. Add a cancellation test: with a `MockEngine` held on a suspension point, cancel the calling coroutine and assert the `CancellationException` reaches the caller and **no** `Result` is emitted — the one path where an over-broad catch turns a cancelled request into stale error state, and one the build's lint tier reports without failing. Add a Koin graph smoke test resolving every declaration including the repository — the accepted cost of runtime DI is that only a test catches a missing binding.
- [x] 23. Update `.claude/CLAUDE.md`: the "Tech stack & versions" section (the catalog is no longer lint-only; Ktor / kotlinx.serialization / coroutines / Koin are now the stack), the "Key patterns" section (add the Koin graph and the `Result<T>` retention, and refresh the Repository-pattern entry, whose example is the `LoginRepository` this branch deletes), the CI section (the new partition entry and the submodule checkout), and the "Documented config files" list (`.gitmodules`, the vendored contract, the CI workflow change).
- [x] 24. Write the DI-framework ADR in the `astro-plans` repo's `adr/` directory following the existing `ADR-<topic>.md` naming: the candidates considered (Koin, kotlin-inject, Metro, manual constructor injection), the decision, and the consequences — runtime resolution with no compile-time graph validation, the graph-resolution smoke test that compensates, and the iOS-facade constraint that keeps Koin off the framework's public surface. Commit it in that repo, not this one.
- [x] 25. Re-point issue #100's trigger: its PR-blocking Gradle SCA (SBOM-based) gate is written as "for M-2 when real product dependencies land", and those dependencies land here. Update the issue to name this branch as the trigger. The gate itself stays deferred — implementing it is not in this branch's scope.

## 5. Testing & Validation (for agent)

Verification commands, paired to the §4 items. `./gradlew :shared:build` and `./gradlew check` both aggregate `iosX64Test`, which the shared build disables on Apple Silicon — so on an arm64 Mac `iosSimulatorArm64Test` is the valid iOS signal.

- [x] 1–2. `./gradlew :shared:compileKotlinIosSimulatorArm64 :shared:compileAndroidMain` succeeds — both engines resolve on their own target. (The Android compile task is `compileAndroidMain`, not `compileKotlinAndroid`; the AGP KMP library plugin names it after the compilation, not the Kotlin convention.) Compilation alone does not prove the serialization plugin *generates* — a plugin that failed to load would still compile source with no `@Serializable` in it — so additionally confirm a `@Serializable` declaration produces a `$$serializer` class, and confirm the Darwin engine links natively via `./gradlew :shared:linkDebugFrameworkIosSimulatorArm64` rather than inferring linkage from a klib compile. Confirm the catalog change alone did not break the lint toolchain: `./gradlew :shared:verifyKtfmtAlignment`.
- [x] 3. `./gradlew :androidApp:assembleDebug` succeeds; confirm `android.permission.INTERNET` appears in the merged manifest at `androidApp/build/intermediates/merged_manifests/debug/processDebugManifest/AndroidManifest.xml`. Read the merged file rather than the source manifest — the merger is what could drop the permission, and the same file confirms the pre-existing `READ_PHONE_STATE` `tools:node="remove"` still takes effect.
- [x] 4. `git submodule status` reports `docs/astro-docs` at a commit; `diff contracts/astro-bff/openapi.yaml docs/astro-docs/openapi.yaml` is empty. Confirm a clone without `--recursive` still *configures* the build (item 6's message path, not a hard configuration failure).
- [x] 5. Read back the workflows: every checkout in both workflows says `submodules: false`, and only `verify-android-common` carries the ssh-agent + `git submodule update --init --recursive` pair. Confirm the credential the job depends on actually exists rather than assuming it: `gh secret list --repo jitrapon/astro-mobile` shows `ASTRO_DOCS_DEPLOY_KEY`, and `gh api repos/jitrapon/astro-docs/keys` shows a matching **read-only** deploy key — a write-capable key here would hand CI push access to the docs repo. Run `./scripts/check-action-pins.sh` so the newly-added third-party action is confirmed SHA-pinned, and parse both workflows as YAML so a malformed edit fails here rather than on the next push.
- [x] 6. `./gradlew verifyVendoredContractParity` passes as committed; then temporarily perturb the vendored copy by one byte and confirm it fails naming the file, and temporarily move `docs/astro-docs` aside and confirm the failure message names the `git submodule update --init` remedy. Perturb the **fixture** as well — the two artifacts are compared by different means (bytes vs. parsed JSON), so a green byte comparison is no evidence the JSON one bites. Revert all perturbations.
- [x] 7. `./gradlew verifyCheckPartition` reports "CI partition OK" with the action-bearing task count incremented by one. This is the item's real gate — the guard fails outright if the task was wired into `check` but not into an aggregate.
- [x] 8. `./gradlew :shared:testAndroidHostTest` and `./gradlew :shared:iosSimulatorArm64Test` both compile the generated source. Confirm the generator is incremental: touch the vendored fixture, re-run, confirm the constant changed; re-run unchanged, confirm `UP-TO-DATE`.
- [x] 9. Assert on the extraction itself, not just that it ran: the embedded parameter set contains `knownTheme` at all (proving the `$ref` into `components.parameters` was resolved — the contract declares it `required: false`, so it lands in the optional group, not the required one), `dayCount` is listed as optional rather than required, `start` and `end` carry `format: date` (the fact item 21's wire-format assertions rest on — extract it as empty and those assertions pass vacuously), and the embedded response facts include an `AgendaBody` entry (proving the discriminator mapping was followed, not just the inline month schema). Also prove the extraction fails loudly instead of emitting a hole: temporarily rename the shared `KnownTheme` parameter, then the `calendar.agenda.v1` mapping target, in the vendored contract and confirm the generator fails naming each. Revert both.
- [x] 10–12. `./gradlew :shared:compileKotlinIosSimulatorArm64` plus `./gradlew :shared:detekt` — the models compile on both targets and carry no Detekt findings. Model *correctness* is proven by item 19's decode and item 21's conformance check, not by compilation. The agenda models have no fixture, so item 21 is their only mechanical signal.
- [x] 13. `./gradlew :shared:detekt` passes with the coroutine ruleset active (no `commonMain` dispatcher/blocking finding), and `semgrep --config .semgrep/astro-mobile.yml --config .semgrep/coroutines.yml shared/` reports no `kotlin-cleartext-http-url` or `kotlin-hardcoded-dispatcher` finding.
- [x] 14–15. Covered by the schema-version and repository `MockEngine` cases in the item-22 run; neither the version guard nor the repository's forwarding contract has a compile-time signal, so a green build is not evidence for these items.
- [x] 16–17. `./gradlew :androidApp:assembleDebug` succeeds with the `Application` registered; `./gradlew :shared:linkDebugFrameworkIosSimulatorArm64` then `xcodebuild -project iosApp/iosApp.xcodeproj -scheme iosApp -destination 'generic/platform=iOS Simulator' build` succeeds; and `./gradlew :shared:iosSimulatorArm64Test` runs the `iosTest` facade test green. Then run **both** runtime loops — `android-device-debug` on an emulator and `ios-device-debug` on a simulator — and confirm from the screenshot that each app reaches the state item 18 renders from the resolved repository. Neither substitutes for the other: a manifest-registered `Application` that throws in `startKoin` crashes at launch while `assembleDebug` stays green, and a broken Swift call site or framework export compiles and links cleanly. Automated instrumentation coverage of app start is deliberately not added here — `androidApp` has no `androidTest` source set and CI runs no emulator, so standing up that harness is its own task (the `testing-setup` skill's domain), not a rider on this branch.
- [x] 18. `grep -rn "Greeting\|LoginRepository\|LoginDataSource\|LoginDataValidator\|LoggedInUser" shared/src androidApp/src iosApp/iosApp` returns nothing; `./gradlew :shared:testAndroidHostTest :shared:iosSimulatorArm64Test` and the `xcodebuild` command above all still succeed. `./gradlew swiftFormatCheck swiftLintCheck` passes on the rewritten Swift.
- [x] 19–22. `./gradlew :shared:testAndroidHostTest` and `./gradlew :shared:iosSimulatorArm64Test` — the parity, mutation, conformance, behaviour, repository, and Koin-resolution tests pass on both the JVM host and the iOS simulator. For item 20, confirm each mutation fails for the reason claimed by inspecting the assertion message, not merely that the suite is green. For item 21, prove the check is genuinely contract-derived on both sides: temporarily rename a query parameter in the vendored contract and confirm the request assertion fails, and temporarily add a field to `AgendaBody`'s `required:` list and confirm the response assertion fails. Then prove the wire-format half bites: temporarily switch the client to serialize `start` as a date-time and confirm the conformance test fails on the encoding, not merely on the name. Revert all three.
- [x] 23–24. Re-read the edited `.claude/CLAUDE.md` sections against the final build files and CI workflow so no statement describes the pre-branch state; confirm the ADR file exists in `astro-plans/adr/` and is committed in that repo.
- [x] 25. `gh issue view 100` shows the re-pointed trigger.
- [x] 26. Final gate: `./gradlew ktfmtFormat` then `./gradlew check` passes end-to-end on an Apple Silicon host, and `./gradlew verifyAndroidCommon` passes (the half CI's Linux runner executes).

## 6. Deployment

Not applicable.

<Otherwise: deployment steps, feature flags, migration ordering, rollback plan.>

## 7. Documentation

- `.claude/CLAUDE.md` — the "Tech stack & versions" section currently states the version catalog is deliberately partial and holds only lint-toolchain versions. This branch adds the first product dependencies, so that description and the tech-stack list need updating.
- An architecture decision record for the DI framework choice, in the planning repo alongside the project's other ADRs.

## 8. References

- Issue: https://github.com/jitrapon/astro-mobile/issues/115
- M-1 part 1 (module restructure): https://github.com/jitrapon/astro-mobile/pull/101
- SCA gate issue whose trigger this branch fires: https://github.com/jitrapon/astro-mobile/issues/100
- Milestone plan (mobile task table, M-1 / M-2): `astro-plans/current-plan.md`
